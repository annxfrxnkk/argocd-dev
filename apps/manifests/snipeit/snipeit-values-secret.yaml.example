# Before applying argo/snipeit-app.yaml, update apps/helm/snipeit/values.yaml
# with real values for the following fields (never commit real values):
#
#   snipeit.config.snipeit.key      - Laravel APP_KEY (base64:<32-byte-random>)
#   snipeit.mysql.mysqlPassword     - MySQL password for the snipeit user
#
# Generate APP_KEY:
#   echo "base64:$(openssl rand -base64 32)"
#
# The wildcard-scigames TLS secret must also be present in the snipeit namespace.
# Copy it with:
#   kubectl get secret wildcard-scigames -n traefik-system -o yaml \
#     | sed 's/namespace: traefik-system/namespace: snipeit/' \
#     | kubectl apply -f -
